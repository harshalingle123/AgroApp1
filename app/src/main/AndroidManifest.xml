<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.codeprince.agroapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Setting" />
        <activity android:name=".Profile" />
        <activity android:name=".Help" />
        <activity android:name=".About" />
        <activity android:name=".Logout" />
        <activity android:name=".AddCrops" />
        <activity android:name=".CropDisesSolution" />
        <activity android:name=".Market" />
        <activity android:name=".Financial" />
        <activity android:name=".CropsInfo" />
        <activity android:name=".Shopping" />
        <activity android:name=".CropsYear"></activity>
    </application>

</manifest>